# OpenAI / Codex Skill Wrapper — cli-qa

- execute `../../shared/skills/cli-qa/`;
- run `orbiads` commands via Bash with `--json` flag;
- use this skill for compliance checks and dry-run validation via CLI.
