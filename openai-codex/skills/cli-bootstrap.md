# OpenAI / Codex Skill Wrapper — cli-bootstrap

- load `../../shared/agents/cli-bootstrap/` for routing and memory;
- execute `../../shared/skills/cli-bootstrap/`;
- run `orbiads` commands via Bash with `--json` flag;
- use this skill when tenant or network context must be verified via CLI.
