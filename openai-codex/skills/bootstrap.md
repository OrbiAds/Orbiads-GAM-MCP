# OpenAI / Codex Skill Wrapper — bootstrap

- load `../../shared/agents/bootstrap/`;
- execute `../../shared/skills/bootstrap/`;
- use this skill before any downstream action when network context is uncertain.