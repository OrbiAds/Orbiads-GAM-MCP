# OpenAI / Codex Skill Wrapper — cli-inventory

- load `../../shared/agents/cli-inventory/` for routing and memory;
- execute `../../shared/skills/cli-inventory/`;
- run `orbiads` commands via Bash with `--json` flag;
- use this skill for ad-unit discovery and inventory audit via CLI.
