# Codex Example — Order to Preview

Prompt:

- "Create or reuse the advertiser and order, prepare line items, then stop after QA previews are ready for review."

Expected route:

- `advertiser-order-line-items` -> `qa-preview`.