# Codex Example — Bootstrap and Network

Prompt:

- "Confirm the tenant, verify credentials, show available GAM networks if needed, and stop after the network context is stable."

Expected route:

- `bootstrap`.