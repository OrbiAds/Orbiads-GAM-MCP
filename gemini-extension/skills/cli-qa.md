# Gemini Skill Wrapper — cli-qa

- load `../../shared/agents/cli-qa/` for routing and memory;
- execute `../../shared/skills/cli-qa/`;
- run `orbiads` commands via shell with `--json` flag;
- use this skill for compliance checks and dry-run validation via CLI.
