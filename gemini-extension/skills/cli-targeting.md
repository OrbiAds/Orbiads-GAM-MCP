# Gemini Skill Wrapper — cli-targeting

- load `../../shared/agents/cli-targeting/` for routing and memory;
- execute `../../shared/skills/cli-targeting/`;
- run `orbiads` commands via shell with `--json` flag;
- use this skill for placement and targeting configuration via CLI.
