# Gemini Skill Wrapper — cli-forecast

- load `../../shared/agents/cli-forecast/` for routing and memory;
- execute `../../shared/skills/cli-forecast/`;
- run `orbiads` commands via shell with `--json` flag;
- use this skill to check supply availability via CLI.
