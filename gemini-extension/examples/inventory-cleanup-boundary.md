# Gemini Example — Inventory Cleanup Boundary

Prompt:

- "Show inactive ad-unit candidates and stop before any destructive cleanup path."

Expected route:

- `inventory-ad-units` with candidate discovery only.