# Handoff Formatter

- return only confirmed IDs, summaries, blockers, approvals, and next skill;
- keep packets short enough to be pasted across wrappers;
- separate `facts`, `risks`, and `nextRecommendedSkill` explicitly.