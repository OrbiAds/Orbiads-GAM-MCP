# Guardrail Reminder

- prefer read-only when it answers the question;
- require preview or `dry_run` before real writes;
- require QA and preview before activation;
- require explicit confirmation before destructive or billed paths.