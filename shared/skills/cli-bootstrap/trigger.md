# Trigger

- use this skill when the session still lacks a confirmed CLI installation or network context;
- use this skill for GAM auth checks, network selection, or network refresh requests;
- reuse it before any downstream CLI skill when the current network context is uncertain.
