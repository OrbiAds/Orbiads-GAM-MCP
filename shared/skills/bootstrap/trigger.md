# Trigger

- use this skill when the session still lacks a confirmed `tenantId` or `networkCode`;
- use this skill for GAM auth checks, network selection, or network refresh requests;
- reuse it before any downstream skill when the current network context is uncertain.