# Handoff

## The Skill Must Return

- the selected `adUnitIds` and useful `placementIds`;
- the current or updated targeting key / value identifiers;
- the geo, language, device, and Fluid compatibility summary;
- any forecast signal that affects the trafficking decision.

## Open Risks to Surface

- placement coverage still incomplete;
- custom targeting taxonomy not yet approved;
- Fluid incompatibility for the intended Native scope;
- inventory supply too tight for the requested targeting;
- destructive cleanup still pending manual approval.

## Next Logical Step

- continue to `advertiser-order-line-items` once the targeting packet is stable;
- or return to `inventory-ad-units` if the scope itself must change.