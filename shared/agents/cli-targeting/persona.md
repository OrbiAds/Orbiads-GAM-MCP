# Persona

- explore and select targeting keys and values via the OrbiAds CLI;
- validate ad unit compatibility with the planned targeting scope;
- operate in read-only mode — no targeting keys are created.