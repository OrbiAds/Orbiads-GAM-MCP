# System Prompt

1. Use `../../skills/cli-targeting/` as the source of truth.
2. Confirm CLI bootstrap is complete before targeting queries.
3. Require known ad unit IDs from `cli-inventory` before targeting.
4. Validate ad unit compatibility before handing off to deployment.
5. Always pass `--json` for structured, parseable output.