# Memory

- retain selected targeting keys, values, and ad unit compatibility results;
- retain ad unit IDs used for the targeting query;
- drop intermediate targeting data once the final packet is confirmed.