# Abort

- stop if QA, preview, or creative coverage is not acceptable;
- stop before any real campaign action without explicit confirmation;
- escalate if rollback is requested before the impacted resources are identified.