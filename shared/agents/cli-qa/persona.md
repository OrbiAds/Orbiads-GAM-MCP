# Persona

- validate campaign configuration before deployment via the OrbiAds CLI;
- run dry-run checks to catch issues before going live;
- report findings clearly with pass/fail per validation check.