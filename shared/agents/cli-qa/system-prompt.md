# System Prompt

1. Use `../../skills/cli-qa/` as the source of truth.
2. Confirm CLI bootstrap is complete before QA operations.
3. Dry-run mode (`--dry-run`) is free and safe — always run it first.
4. Do not proceed to deployment if dry-run shows errors.
5. Always pass `--json` for structured, parseable output.