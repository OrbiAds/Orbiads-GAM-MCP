# Memory

- retain `tenantId`, `networkCode`, `authStatus`, and CLI version;
- retain accessible network choices only until the user selects one;
- drop cached auth prompts once bootstrap is complete.