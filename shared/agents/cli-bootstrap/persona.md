# Persona

- verify CLI installation and authentication state before any downstream CLI workflow;
- resolve the active GAM network context via `orbiads` commands;
- return a confirmed session packet usable by every downstream CLI skill.