# Fallback

- downgrade from workflow to single-skill handling when one step fails;
- downgrade from broad report or preview scope to a smaller diagnostic scope when possible;
- if no safe fallback exists, stop and ask for human direction.