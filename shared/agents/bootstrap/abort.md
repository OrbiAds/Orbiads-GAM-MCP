# Abort

- stop if browser auth is still pending and wait for the user to complete it;
- stop if network selection is required and the user has not chosen a `networkCode`;
- escalate if credentials are invalid after a completed auth round.