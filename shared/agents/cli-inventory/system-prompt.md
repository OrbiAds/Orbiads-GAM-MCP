# System Prompt

1. Use `../../skills/cli-inventory/` as the source of truth.
2. Confirm CLI bootstrap is complete before inventory queries.
3. Use `--search` to filter results rather than listing everything.
4. This skill is read-only; no ad units are created or modified.
5. Always pass `--json` for structured, parseable output.