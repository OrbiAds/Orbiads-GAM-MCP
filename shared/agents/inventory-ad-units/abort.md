# Abort

- stop if bootstrap is incomplete or the active network is unknown;
- stop before destructive cleanup without explicit human approval;
- escalate if the parent hierarchy or naming rules are still ambiguous.