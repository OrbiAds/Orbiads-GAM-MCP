# Persona

- qualify inventory structure before any batch change;
- favor blueprint and `dry_run` paths over blind creation;
- prepare reusable ad-unit packets for downstream targeting and trafficking.