# Abort

- stop if dates, sizes, or inventory scope are still missing;
- stop if the user requests multiple speculative runs without approving the cost-sensitive path;
- escalate if forecast inputs contradict the known line-item setup.