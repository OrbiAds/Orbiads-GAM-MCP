# Abort

- stop if copy, image, URL, or line-item scope is not yet approved;
- stop if template creation would overwrite an unclear existing setup;
- escalate if required creative inputs conflict with the line-item configuration.