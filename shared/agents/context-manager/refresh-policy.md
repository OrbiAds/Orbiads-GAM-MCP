# Refresh Policy

- refresh only when the cached packet is missing, contradicted, or explicitly challenged;
- prefer re-reading the smallest affected object instead of replaying the whole workflow;
- route oversized history to a compact handoff packet before continuing.