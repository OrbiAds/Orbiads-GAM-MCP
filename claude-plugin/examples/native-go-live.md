# Claude Example — Native Go Live

User prompt:

- "Turn this approved image asset into a Native creative, run QA plus preview, then prepare a deployment dry run."

Expected route:

- `native-image` -> `qa-preview` -> `deploy-reporting`.