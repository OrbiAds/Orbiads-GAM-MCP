# Claude Example — Bootstrap Session

User prompt:

- "Connect me to the right GAM network, confirm the active tenant, and stop before any write action."

Expected route:

- `bootstrap` -> session packet with `tenantId`, `networkCode`, and next recommended skill.