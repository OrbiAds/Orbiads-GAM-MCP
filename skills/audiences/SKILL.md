---
name: orbiads-audiences
description: Parent MCP tool for OrbiAds GAM — audiences. Parent audiences tool for the Epic 68.2 catalogue refactor batch Dispatches 5 actions via the `action` discriminator. Mode: mixed (free reads + paid writes). 5 legacy child tool(s) still route to this parent — see legacy-tool-mapping.md. Use when the orchestrator narrows the user task to this specific domain.
metadata:
  parent_mcp_tool: audiences
  module: audiences
  source_path: backend/src/mcp/tools/audiences.py
  source_line: 219
  epic: '68.2'
  action_count: 5
  read_only: false
user-invokable: false
---

<!--
  ⚠️ DO NOT EDIT — generated by scripts/generate_skills/generate.py
  Source: backend/src/mcp/tools/audiences.py
  Regenerate: python scripts/generate_skills/generate.py --write
-->

# `audiences` — sub-skill (Epic 68.2)

_Parent audiences tool for the Epic 68.2 catalogue refactor batch._

**Mode:** mixed (read + write) · **Actions:** 5 (3 writes, 2 reads)

**Source:** [`backend/src/mcp/tools/audiences.py:219`](backend/src/mcp/tools/audiences.py#L219)

**Legacy wrappers:** 5 pre-refactor child tool(s) still route to this parent. New integrations should use the parent — see [`../../_docs/legacy-tool-mapping.md`](../../_docs/legacy-tool-mapping.md).

## Actions

| Action | Cost | Write? | Confirmation token |
|---|---|---|---|
| `create_audience_segment` | 0.5 | yes | required |
| `perform_audience_segment_action` | 0 | yes | — |
| `update_audience_segment` | 0 | yes | — |
| `get_audience_segment` | 0 | — | — |
| `list_audience_segments` | 0 | — | — |

## Write protocol

Every write follows the **preview → confirm → execute** pattern enforced by `billing_guard` + `confirmation_token` (SHA-256 over the payload, 5-minute TTL):

1. Call with `dry_run=true` (or without `confirmation_token`) — server returns the preview + token + estimated cost.
2. Show the preview to the user. Get explicit consent.
3. Call again with the same payload + `confirmation_token=<from step 1>`. Server validates token freshness + payload SHA match.

Token drift (any payload field changed between preview and execute) returns `IDEMPOTENCY_KEY_MISMATCH`. Refresh by re-running step 1.

## Routing back to the orchestrator

This sub-skill is `user-invokable: false`. The `orbiads` orchestrator ([`../orbiads/SKILL.md`](../orbiads/SKILL.md)) routes intents to this parent based on its Quick Reference table. If you load this file directly, you bypass the orchestrator's context intake — re-route through the orchestrator unless you already know exactly which action to call.
