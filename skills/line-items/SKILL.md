---
name: orbiads-line-items
description: Parent MCP tool for OrbiAds GAM — line_items. Parent line_items tool for non-lifecycle Line Item operations Dispatches 15 actions via the `action` discriminator. Mode: mixed (free reads + paid writes). 15 legacy child tool(s) still route to this parent — see legacy-tool-mapping.md. Use when the orchestrator narrows the user task to this specific domain.
metadata:
  parent_mcp_tool: line_items
  module: line_items
  source_path: backend/src/mcp/tools/line_items.py
  source_line: 217
  epic: '68'
  action_count: 15
  read_only: false
user-invokable: false
---

<!--
  ⚠️ DO NOT EDIT — generated by scripts/generate_skills/generate.py
  Source: backend/src/mcp/tools/line_items.py
  Regenerate: python scripts/generate_skills/generate.py --write
-->

# `line_items` — sub-skill (Epic 68)

_Parent line_items tool for non-lifecycle Line Item operations._

**Mode:** mixed (read + write) · **Actions:** 15 (11 writes, 4 reads)

**Source:** [`backend/src/mcp/tools/line_items.py:217`](backend/src/mcp/tools/line_items.py#L217)

**Legacy wrappers:** 15 pre-refactor child tool(s) still route to this parent. New integrations should use the parent — see [`../../_docs/legacy-tool-mapping.md`](../../_docs/legacy-tool-mapping.md).

## Actions

| Action | Cost | Write? | Confirmation token |
|---|---|---|---|
| `activate_batch` | 0 | yes | — |
| `approve` | 0 | yes | — |
| `archive` | 0 | yes | — |
| `create_adexchange` | 0 | yes | — |
| `create_batch` | 0 | yes | — |
| `create_open_bidding` | 0 | yes | — |
| `create_preferred_deal` | 0 | yes | — |
| `duplicate` | 0 | yes | — |
| `pause_batch` | 0 | yes | — |
| `update` | 0 | yes | — |
| `update_targeting` | 0 | yes | — |
| `get` | 0 | — | — |
| `list_by_order` | 0 | — | — |
| `list_private_deals` | 0 | — | — |
| `verify` | 0 | — | — |

## Write protocol

Every write follows the **preview → confirm → execute** pattern enforced by `billing_guard` + `confirmation_token` (SHA-256 over the payload, 5-minute TTL):

1. Call with `dry_run=true` (or without `confirmation_token`) — server returns the preview + token + estimated cost.
2. Show the preview to the user. Get explicit consent.
3. Call again with the same payload + `confirmation_token=<from step 1>`. Server validates token freshness + payload SHA match.

Token drift (any payload field changed between preview and execute) returns `IDEMPOTENCY_KEY_MISMATCH`. Refresh by re-running step 1.

## Routing back to the orchestrator

This sub-skill is `user-invokable: false`. The `orbiads` orchestrator ([`../orbiads/SKILL.md`](../orbiads/SKILL.md)) routes intents to this parent based on its Quick Reference table. If you load this file directly, you bypass the orchestrator's context intake — re-route through the orchestrator unless you already know exactly which action to call.
