---
name: orbiads-inventory
description: Parent MCP tool for OrbiAds GAM — inventory. Parent inventory tool for Story 68.6 Dispatches 10 actions via the `action` discriminator. Mode: mixed (free reads + paid writes). 10 legacy child tool(s) still route to this parent — see legacy-tool-mapping.md. Use when the orchestrator narrows the user task to this specific domain.
metadata:
  parent_mcp_tool: inventory
  module: inventory
  source_path: backend/src/mcp/tools/inventory.py
  source_line: 406
  epic: '68.6'
  action_count: 10
  read_only: false
user-invokable: false
---

<!--
  ⚠️ DO NOT EDIT — generated by scripts/generate_skills/generate.py
  Source: backend/src/mcp/tools/inventory.py
  Regenerate: python scripts/generate_skills/generate.py --write
-->

# `inventory` — sub-skill (Epic 68.6)

_Parent inventory tool for Story 68.6._

**Mode:** mixed (read + write) · **Actions:** 10 (3 writes, 7 reads)

**Source:** [`backend/src/mcp/tools/inventory.py:406`](backend/src/mcp/tools/inventory.py#L406)

**Legacy wrappers:** 10 pre-refactor child tool(s) still route to this parent. New integrations should use the parent — see [`../../_docs/legacy-tool-mapping.md`](../../_docs/legacy-tool-mapping.md).

## Actions

| Action | Cost | Write? | Confirmation token |
|---|---|---|---|
| `archive_inactive_ad_units` | 0 | yes | — |
| `create_ad_units_batch` | 0.5 | yes | required |
| `push_inventory_blueprint` | 0.5 | yes | required |
| `audit_inventory` | 0 | — | — |
| `find_inactive_ad_units` | 0.25 | — | — |
| `generate_ads_json` | 0 | — | — |
| `generate_inventory_blueprint` | 0 | — | — |
| `get_ad_unit_tree` | 0 | — | — |
| `get_ad_units_by_ids` | 0 | — | — |
| `list_ad_unit_sizes` | 0 | — | — |

## Write protocol

Every write follows the **preview → confirm → execute** pattern enforced by `billing_guard` + `confirmation_token` (SHA-256 over the payload, 5-minute TTL):

1. Call with `dry_run=true` (or without `confirmation_token`) — server returns the preview + token + estimated cost.
2. Show the preview to the user. Get explicit consent.
3. Call again with the same payload + `confirmation_token=<from step 1>`. Server validates token freshness + payload SHA match.

Token drift (any payload field changed between preview and execute) returns `IDEMPOTENCY_KEY_MISMATCH`. Refresh by re-running step 1.

## Routing back to the orchestrator

This sub-skill is `user-invokable: false`. The `orbiads` orchestrator ([`../orbiads/SKILL.md`](../orbiads/SKILL.md)) routes intents to this parent based on its Quick Reference table. If you load this file directly, you bypass the orchestrator's context intake — re-route through the orchestrator unless you already know exactly which action to call.
