---
name: orbiads-creative-assets
description: Parent MCP tool for OrbiAds GAM — creative_assets. Parent creative asset tool for upload/create/compress/transcode actions Dispatches 15 actions via the `action` discriminator. Mode: mixed (free reads + paid writes). 14 legacy child tool(s) still route to this parent — see legacy-tool-mapping.md. Use when the orchestrator narrows the user task to this specific domain.
metadata:
  parent_mcp_tool: creative_assets
  module: creatives
  source_path: backend/src/mcp/tools/creatives.py
  source_line: 768
  epic: '68.7d'
  action_count: 15
  read_only: false
user-invokable: false
---

<!--
  ⚠️ DO NOT EDIT — generated by scripts/generate_skills/generate.py
  Source: backend/src/mcp/tools/creatives.py
  Regenerate: python scripts/generate_skills/generate.py --write
-->

# `creative_assets` — sub-skill (Epic 68.7d)

_Parent creative asset tool for upload/create/compress/transcode actions._

**Mode:** mixed (read + write) · **Actions:** 15 (12 writes, 3 reads)

**Source:** [`backend/src/mcp/tools/creatives.py:768`](backend/src/mcp/tools/creatives.py#L768)

**Legacy wrappers:** 14 pre-refactor child tool(s) still route to this parent. New integrations should use the parent — see [`../../_docs/legacy-tool-mapping.md`](../../_docs/legacy-tool-mapping.md).

## Actions

| Action | Cost | Write? | Confirmation token |
|---|---|---|---|
| `create_audio` | 0 | yes | — |
| `create_classic_native` | 0 | yes | — |
| `create_companion` | 0 | yes | — |
| `create_html5` | 0 | yes | — |
| `create_html5_from_files` | 0 | yes | — |
| `create_image` | 0 | yes | — |
| `create_third_party` | 0 | yes | — |
| `create_vast_redirect` | 0 | yes | — |
| `create_video` | 0 | yes | — |
| `upload_and_associate` | 0 | yes | — |
| `upload_from_url` | 0 | yes | — |
| `upload_html5_zip` | 0 | yes | — |
| `bulk_upload` | 0 | — | — |
| `compress_image` | 0 | — | — |
| `get_video_transcode_status` | 0 | — | — |

## Write protocol

Every write follows the **preview → confirm → execute** pattern enforced by `billing_guard` + `confirmation_token` (SHA-256 over the payload, 5-minute TTL):

1. Call with `dry_run=true` (or without `confirmation_token`) — server returns the preview + token + estimated cost.
2. Show the preview to the user. Get explicit consent.
3. Call again with the same payload + `confirmation_token=<from step 1>`. Server validates token freshness + payload SHA match.

Token drift (any payload field changed between preview and execute) returns `IDEMPOTENCY_KEY_MISMATCH`. Refresh by re-running step 1.

## Routing back to the orchestrator

This sub-skill is `user-invokable: false`. The `orbiads` orchestrator ([`../orbiads/SKILL.md`](../orbiads/SKILL.md)) routes intents to this parent based on its Quick Reference table. If you load this file directly, you bypass the orchestrator's context intake — re-route through the orchestrator unless you already know exactly which action to call.
