---
name: orbiads-companies
description: Parent MCP tool for OrbiAds GAM — companies. Companies dispatcher — single entry point for advertisers, agencies, and contacts Dispatches 12 actions via the `action` discriminator (or `(area, action)` for 2D dispatch). Mode: mixed (free reads + paid writes). 12 legacy child tool(s) still route to this parent — see legacy-tool-mapping.md. Use when the orchestrator narrows the user task to this specific domain.
metadata:
  parent_mcp_tool: companies
  module: advertisers
  source_path: backend/src/mcp/tools/advertisers.py
  source_line: 225
  action_count: 12
  read_only: false
user-invokable: false
---

<!--
  ⚠️ DO NOT EDIT — generated by scripts/generate_skills/generate.py
  Source: backend/src/mcp/tools/advertisers.py
  Regenerate: python scripts/generate_skills/generate.py --write
-->

# `companies` — sub-skill

_Companies dispatcher — single entry point for advertisers, agencies, and contacts._

**Mode:** mixed (read + write) · **Actions:** 12 (6 writes, 6 reads)

**Source:** [`backend/src/mcp/tools/advertisers.py:225`](backend/src/mcp/tools/advertisers.py#L225)

**Legacy wrappers:** 12 pre-refactor child tool(s) still route to this parent. New integrations should use the parent — see [`../../_docs/legacy-tool-mapping.md`](../../_docs/legacy-tool-mapping.md).

## Actions

| Action | Cost | Write? | Confirmation token |
|---|---|---|---|
| `advertisers.create` | 0 | yes | — |
| `advertisers.update` | 0 | yes | — |
| `agencies.create` | 0 | yes | — |
| `agencies.update` | 0 | yes | — |
| `contacts.create` | 0 | yes | — |
| `contacts.update` | 0 | yes | — |
| `advertisers.find` | 0 | — | — |
| `advertisers.find_or_create` | 0 | — | — |
| `advertisers.get` | 0 | — | — |
| `advertisers.list` | 0 | — | — |
| `agencies.list` | 0 | — | — |
| `contacts.list` | 0 | — | — |

## Write protocol

Every write follows the **preview → confirm → execute** pattern enforced by `billing_guard` + `confirmation_token` (SHA-256 over the payload, 5-minute TTL):

1. Call with `dry_run=true` (or without `confirmation_token`) — server returns the preview + token + estimated cost.
2. Show the preview to the user. Get explicit consent.
3. Call again with the same payload + `confirmation_token=<from step 1>`. Server validates token freshness + payload SHA match.

Token drift (any payload field changed between preview and execute) returns `IDEMPOTENCY_KEY_MISMATCH`. Refresh by re-running step 1.

## Routing back to the orchestrator

This sub-skill is `user-invokable: false`. The `orbiads` orchestrator ([`../orbiads/SKILL.md`](../orbiads/SKILL.md)) routes intents to this parent based on its Quick Reference table. If you load this file directly, you bypass the orchestrator's context intake — re-route through the orchestrator unless you already know exactly which action to call.
