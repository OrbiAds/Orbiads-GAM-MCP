---
name: orbiads-settings
description: Parent MCP tool for OrbiAds GAM — settings. Parent settings tool for the Epic 68.1 catalogue refactor POC Dispatches 9 actions via the `action` discriminator. Mode: mixed (free reads + paid writes). 9 legacy child tool(s) still route to this parent — see legacy-tool-mapping.md. Use when the orchestrator narrows the user task to this specific domain.
metadata:
  parent_mcp_tool: settings
  module: settings
  source_path: backend/src/mcp/tools/settings.py
  source_line: 314
  epic: '68.1'
  action_count: 9
  read_only: false
user-invokable: false
---

<!--
  ⚠️ DO NOT EDIT — generated by scripts/generate_skills/generate.py
  Source: backend/src/mcp/tools/settings.py
  Regenerate: python scripts/generate_skills/generate.py --write
-->

# `settings` — sub-skill (Epic 68.1)

_Parent settings tool for the Epic 68.1 catalogue refactor POC._

**Mode:** mixed (read + write) · **Actions:** 9 (5 writes, 4 reads)

**Source:** [`backend/src/mcp/tools/settings.py:314`](backend/src/mcp/tools/settings.py#L314)

**Legacy wrappers:** 9 pre-refactor child tool(s) still route to this parent. New integrations should use the parent — see [`../../_docs/legacy-tool-mapping.md`](../../_docs/legacy-tool-mapping.md).

## Actions

| Action | Cost | Write? | Confirmation token |
|---|---|---|---|
| `create_preset` | 0 | yes | — |
| `delete_preset` | 0 | yes | — |
| `update_delivery_defaults` | 0 | yes | — |
| `update_naming_conventions` | 0 | yes | — |
| `update_tenant_settings` | 0 | yes | — |
| `get_delivery_defaults` | 0 | — | — |
| `get_naming_conventions` | 0 | — | — |
| `get_tenant_settings` | 0 | — | — |
| `list_presets` | 0 | — | — |

## Write protocol

Every write follows the **preview → confirm → execute** pattern enforced by `billing_guard` + `confirmation_token` (SHA-256 over the payload, 5-minute TTL):

1. Call with `dry_run=true` (or without `confirmation_token`) — server returns the preview + token + estimated cost.
2. Show the preview to the user. Get explicit consent.
3. Call again with the same payload + `confirmation_token=<from step 1>`. Server validates token freshness + payload SHA match.

Token drift (any payload field changed between preview and execute) returns `IDEMPOTENCY_KEY_MISMATCH`. Refresh by re-running step 1.

## Routing back to the orchestrator

This sub-skill is `user-invokable: false`. The `orbiads` orchestrator ([`../orbiads/SKILL.md`](../orbiads/SKILL.md)) routes intents to this parent based on its Quick Reference table. If you load this file directly, you bypass the orchestrator's context intake — re-route through the orchestrator unless you already know exactly which action to call.
