---
description: "Deploy GAM campaigns and monitor delivery via OrbiAds CLI. Use when the user asks to deploy ads, check delivery status, or run reports from the terminal."
---

# OrbiAds CLI — Deploy

Requires `orbiads` CLI installed (`pip install orbiads-cli`).
Run commands via the Bash tool with `--json` flag for structured output.

## Instructions

See [shared skill](../../shared/skills/cli-deploy/) for complete workflow:
- [tools.md](../../shared/skills/cli-deploy/tools.md) — Available CLI commands
- [steps.md](../../shared/skills/cli-deploy/steps.md) — Step-by-step workflow
- [examples.md](../../shared/skills/cli-deploy/examples.md) — Bash examples with JSON output
